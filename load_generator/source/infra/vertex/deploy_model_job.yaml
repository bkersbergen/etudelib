apiVersion: batch/v1
kind: Job
metadata:
  name: "${JOB_NAME}"
spec:
  ttlSecondsAfterFinished: 60
  backoffLimit: 0
  template:
    spec:
      serviceAccountName: reco-analytics
      restartPolicy: Never
      containers:
        - name: "${JOB_NAME}"
          image: "eu.gcr.io/bolcom-stg-pyxle-images-043/vertexai-utilities:latest"
          args: [
            "--project", "bolcom-pro-reco-analytics-fcc",
            "models",
            "deploy",
            "--display-name", "etude-${VERTEX_MODEL}",
            "--container-image-url", "eu.gcr.io/bolcom-pro-reco-analytics-fcc/${VERTEX_MODEL}:latest",
            "--container-port", "7080",
            "--predict-route", "/predictions/${VERTEX_MODEL}",
            "--health-check-route", "/ping",
            "--description", "etude-${VERTEX_MODEL}",
            "--is-default",
          ]
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1001
            seccompProfile:
              type: "RuntimeDefault"

